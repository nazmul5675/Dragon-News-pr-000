import { createBrowserRouter } from 'react-router';

const router = createBrowserRouter([
    {
        path: "/",
        element: <h1 className='btn text-xl bg-red-100 text-red-500'>this is btn</h1>,
    },
]);



export default router;